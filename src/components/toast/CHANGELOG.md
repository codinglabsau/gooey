# Changelog
## v1.0.0
### use-toast.ts
* Added `icon`, `iconClasses`, `messages` to `ToasterToast` type. This allows support for Inertia style toasts through the Notifications component.
* Increased toast limit 3 (was 1)

### Toaster.vue
* Added support messages and icons to template
